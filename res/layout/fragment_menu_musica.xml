<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MenuMusica">

    <!-- TODO: Update blank fragment layout -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_horizontal"
        android:weightSum="1">

        <ImageButton android:id="@+id/musica"
            android:src="@drawable/musica2"
            style="@style/botones2"/>

        <Space
            style="@style/space" />

        <ImageButton android:id="@+id/linterna"
            android:src="@drawable/linterna"
            style="@style/botones"/>
        <Space
            style="@style/space" />

        <ImageButton android:id="@+id/nivel"
            android:src="@drawable/nivel"
            style="@style/botones"/>

    </LinearLayout>

</FrameLayout>